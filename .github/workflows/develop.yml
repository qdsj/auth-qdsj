on:
  push:
    branches: [main]
jobs:
  main:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Start deploy
        uses: ./.github/workflows/deploy.yml
        with:
          branch: develop
          port: '3100'
          docker_name: 'authqdsj'
